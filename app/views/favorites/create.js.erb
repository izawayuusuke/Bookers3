if (location.pathname == "/books/<%= @book.id %>") {
  $(".book_<%= @book.id %>").html("<%= j(render 'books/detail',  book: @book) %>")
} else {
  $(".book_<%= @book.id %>").html("<%= j(render 'favorites/favorites',  book: @book) %>")
}
