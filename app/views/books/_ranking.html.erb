<table class="table">
  <% @ranking_books.each_with_index do |book, i| %>
    <tr>
      <th><span class="ranking-<%= i + 1 %>"><%= i + 1 %>位</span> Title</th>
      <td><%= book.title %></td>
    </tr>
    <tr>
      <th>Opinion</th>
      <td><%= book.body %></td>
    </tr>
  <% end %>
</table>
